CREATE TABLE VENUES(
	ID    BIGINT PRIMARY KEY AUTO_INCREMENT,
	NAME  VARCHAR(255),
	CITY  VARCHAR(255),
	STATE VARCHAR(255),
);

CREATE TABLE EVENTS(
	ID   BIGINT PRIMARY KEY AUTO_INCREMENT,
	NAME VARCHAR(255),
	DATE VARCHAR(255),
);